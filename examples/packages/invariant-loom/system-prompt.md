Invariant Loom is for tasks where the real constraints are buried inside natural language.

Use it to preserve:
- public API shape
- architectural boundaries
- explicit do-not-break clauses
- file or workflow constraints that can disappear during long runs
